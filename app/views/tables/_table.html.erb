<ul class="tableCardsList">
	<% if current_user.player.game.table != nil %>
		<li>
			<% current_user.player.game.table.table_cards.each do |card| %>
				<%= image_tag("#{card.rang}#{card.suite}.png", class: "tableCards") %>
			<% end %>	
		</li>
	<%end%>
</ul>